PTrho_list = []
h_list = [999, 12345, 24242, 36987, 74123]
for h in h_list:
    PTrho_list.append(get_PTrho_given_altitude(h))
PTrho_df = pd.DataFrame(PTrho_list, columns = ["P/lb.ft-2", "Temp / degF", "rho/slug.ft-3"])
PTrho_df.insert(0, "h/ feet", h_list)
print(PTrho_df)
